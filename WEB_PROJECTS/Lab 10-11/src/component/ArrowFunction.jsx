const ArrowFunction = ({ name, age }) => {
    return (
        <div className="card arrow-card">
            <h2>Arrow Function Component (Task 7)</h2>
            <p>Name: <strong>{name}</strong></p>
            <p>Age: <strong>{age}</strong></p>
        </div>
    );
};

export default ArrowFunction;
